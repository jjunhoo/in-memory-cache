<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.amorepacific.inmemorycache.mapper.ProductMapper">
    <!-- 상품 리스트 조회 -->
    <select id="selectProductList" resultType="com.amorepacific.inmemorycache.domain.Product">
        SELECT PRODUCT_NO AS productNo
             , PRODUCT_NAME AS productName
             , BRAND_NAME AS brandName
             , PRODUCT_PRICE AS productPrice
             , CATEGORY_NO AS categoryNo
          FROM PRODUCT
    </select>

    <!-- 상품 조회 -->
    <select id="selectProduct" resultType="com.amorepacific.inmemorycache.domain.Product">
        SELECT PRODUCT_NO AS productNo
             , PRODUCT_NAME AS productName
             , BRAND_NAME AS brandName
             , PRODUCT_PRICE AS productPrice
             , CATEGORY_NO AS categoryNo
          FROM PRODUCT
         WHERE PRODUCT_NO = #{productNo}
    </select>

    <!-- 상품명 수정 -->
    <update id="updateProductName">
        UPDATE PRODUCT
           SET PRODUCT_NAME = #{productName}
         WHERE PRODUCT_NO = #{productNo}
    </update>

    <!-- 상품 가격 수정 -->
    <update id="updateProductPrice">
        UPDATE PRODUCT
           SET PRODUCT_PRICE = #{productPrice}
         WHERE PRODUCT_NO = #{productNo}
    </update>
</mapper>